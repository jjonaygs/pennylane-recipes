<%= turbo_frame_tag "load_more", 
    src: recipes_path(
        page: @recipes.next_page, 
        ingredient_names: params[:ingredient_names], 
        format: :turbo_stream
    ), 
    loading: :lazy %>